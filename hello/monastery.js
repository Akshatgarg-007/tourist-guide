const monastry = document.getElementById("Monastery");

// Add event listener
monastry.addEventListener("click", () => {
    // Redirect to another page
    window.location.href = "monastry.html";
});