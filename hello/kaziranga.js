const kaziranga = document.getElementById("kaziranga");

// Add event listener
kaziranga.addEventListener("click", () => {
    // Redirect to another page
    window.location.href = "kaziranga.html";
});